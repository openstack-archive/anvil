get_from: "git://github.com/openstack/quantum.git?branch=stable/folsom"

patches:
  # After check-out/download time patches
  download:
    - "conf/patches/quantum/download/"
  # At packaging time patches
  package:
    - "conf/patches/quantum/package/"
