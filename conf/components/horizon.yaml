# Settings for component horizon
---
# Where we download this from...
get_from: "git://github.com/openstack/horizon.git?tag=2013.1"

# NOTE(imelnikov): could not make this work on my test machine
tests_package:
    enabled: false

...
